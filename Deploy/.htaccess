<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /clientes/PRJ-0007/
  
  # Se o arquivo ou diretório físico não existir, manda para o index.html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^ index.html [L]
</IfModule>
